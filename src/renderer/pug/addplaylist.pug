
extends ./__layout.pug

block title
  title Add Playlist

block body

  body(class="addplaylist-body")
    header(class="addplaylist-header")
      span Add Playlist
      span(class="fa fa-window-close addplaylist-close")

    section(class="addplaylist")

      a new list

      div(class="addplaylist-newlist" hidden)
        input(type="text")
        i(class="fa fa-close addplaylist-close-input")

      button(class="submitaddplaylist" type="submit") Add
    script(src="../js/addplaylist.js")
