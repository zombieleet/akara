"use strict";

const play = [ "fa-play", "fa-play-circle", "fa-play-circle-o" ];
const stop = [ "fa-stop", "fa-stop-circle", "fa-stop-circle-o" ];
const backward = [ "fa-backward", "fa-fast-backward", "fa-step-backward" ];
const forward = [ "fa-fast-foward", "fa-foward", "fa-step-forward" ];

const filter = [ ];
const random = [ ];
const expand = [ ];
const shrink = [ ];


module.exports = {
    play,
    stop,
    backward,
    forward,
    filter,
    random,
    expand,
    shrink
};
